<nav class="bg-primary-700 text-white shadow-lg">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex items-center">
        <%= link_to root_path, class: "flex items-center" do %>
        <span class="text-xl font-bold">BeverageTracker</span>
        <% end %>
      </div>

      <div class="flex items-center space-x-4">
        <%= link_to "New Entry", new_beverage_entry_path, class: "hover:bg-primary-600 px-3 py-2 rounded-md" %>
        <%= link_to "My Log", beverage_entries_path, class: "hover:bg-primary-600 px-3 py-2 rounded-md" %>

        <div class="relative ml-3">
          <div class="flex items-center space-x-2">
            <span class="text-sm"><%= current_user.email %></span>
            <%= button_to "Sign Out", destroy_user_session_path, method: :delete, 
                class: "bg-secondary-500 hover:bg-secondary-600 px-3 py-1 rounded-md text-sm" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>